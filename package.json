{
  "name": "y",
  "version": "y",
  "description": "azure service bus subscriber",
  "main": "y",
  "author": "thomas",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node build/index"
  },
  "dependencies": {
    "@azure/service-bus": "^1.1.10",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "@dooboo/eslint-config": "^0.5.0"
  }
}
